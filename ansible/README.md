# Ansible

This directory contains Ansible playbooks and roles for configuration management and automation.

- Place playbooks in the root of this directory.
- Place reusable roles in `roles/`.
- Organize inventory files by environment (e.g., `inventory/dev`, `inventory/prod`).
