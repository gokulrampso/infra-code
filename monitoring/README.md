# Monitoring

This directory contains configuration for the monitoring stack, including Grafana, Prometheus, and the ELK stack (Elasticsearch, Logstash, Kibana).

- Place manifests, Helm charts, and dashboards here.
- Organize by tool (e.g., `prometheus/`, `grafana/`, `elk/`).
- Include sample dashboards and alerting rules.
